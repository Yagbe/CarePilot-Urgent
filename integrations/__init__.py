"""
Integration adapters for external systems (e.g. NPHIES/نفيس, payers).

This package intentionally keeps adapters pluggable and minimal so that
real endpoints and credentials can be wired in via environment variables
without changing the core application.
"""

